{
  "name": "emilia-transitions",
  "version": "0.0.1",
  "description": "emilia transitions and animation showcase — the six preset bundles of `§ 15.1` as three declarations from one token, behaviour, the two nine-step ms ladders, the easings as theme references, the four built-in animations with their hoisted `@keyframes` blocks, and a submit button that transitions its colours on hover and swaps to a spinner while the form is in flight",
  "src": "src/",
  "entry": "main.bp",
  "target": "commonJS",
  "targets": ["commonJS", "erlang"],
  "dependencies": {
    "emilia": {
      "workspace": true
    }
  }
}
