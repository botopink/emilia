{
  "name": "emilia-cascade",
  "version": "0.0.1",
  "description": "emilia cascade showcase — one card whose styles reach outside its own class: a hover that is a sibling rule, a breakpoint that is a hoisted @media, a reset supplied through Options, and a layered document a project's own CSS could deliberately beat",
  "src": "src/",
  "entry": "main.bp",
  "target": "commonJS",
  "targets": ["commonJS", "erlang"],
  "dependencies": {
    "emilia": {
      "workspace": true
    }
  }
}
