{
  "name": "emilia-transforms",
  "version": "0.0.1",
  "description": "emilia transforms showcase — `§ 16` as three independent v4 properties that compose, the negative rotations a `Neg` sub-section spells, the two decimal conventions four subsections apart, the perspective family as `--perspective-*` references, the two rows where upstream and the reference file disagree, and a card that lifts and a chevron that flips with front 44's transition behind them",
  "src": "src/",
  "entry": "main.bp",
  "target": "commonJS",
  "targets": ["commonJS", "erlang"],
  "dependencies": {
    "emilia": {
      "workspace": true
    }
  }
}
