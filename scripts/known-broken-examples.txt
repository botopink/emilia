# Examples allowed to fail the examples gate (runExamplesGate in
# scripts/git-hooks/lib/runner-standalone.sh). One `examples/<name>  <reason>`
# per line. A listed example that builds fails the gate — delete its line.
# Owner: 1.0.4-beta front 13 (ecosystem-migration) fixes them.

examples/emilia-card  'h1' expects 2 argument(s), got 1 (also depends on jhonstart, not checked out by CI)
