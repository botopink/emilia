{
  "name": "emilia",
  "version": "0.0.1",
  "description": "emilia core — the `Token` enum surface (variants per CSS family + Hex(string) escape), resolved to flat CSS declarations, registered on a per-render Stylesheet host cell, and flushed alongside renderToString — commonJS and erlang",
  "src": "src/",
  "entry": "root.bp",
  "target": "commonJS",
  "targets": ["commonJS", "erlang"],
  "files": [
    "root.bp",
    "tokens.bp",
    "theme.bp",
    "spacing.bp",
    "output.bp",
    "emilia.bp"
  ]
}
