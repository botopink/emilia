# emilia · CHANGELOG

## Unreleased

- Seed: repository created under `botopink/emilia`. Tracked from
  `botopink/projects` (workspace root) as a git submodule on the `feat`
  branch. Intent + design surface live in
  `tasks/v0.beta.19/specs/emilia.md` of the parent workspace; the runtime is
  added in subsequent commits.
